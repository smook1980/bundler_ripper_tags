require 'spec_helper'

describe BundlerRipperTags do
  it 'has a version number' do
    expect(BundlerRipperTags::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
